export enum NOTIFICATION_AUDIENCE {
  ALL = 'all',
  OPERATORS = 'operators',
  CUSTOMERS = 'customers',
}

export enum NOTIFICATION_STATUS {
  DELIVERED = 'delivered',
  FAILED = 'failed',
}

export enum DEVICE_PLATFORM {
  ANDROID = 'android',
  IOS = 'ios',
}
